{
    "apiKey": {
        "type": "string",
        "label": "API Key",
        "description": "Shared secret checked against the applet's configured API key"
    },
    "parentNoteId": {
        "type": "note",
        "label": "Parent Note",
        "description": "The first child of this note (in Trilium sort order) is surfaced"
    }
}
